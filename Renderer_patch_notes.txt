// ── In Renderer.hpp, add one line to the uniform locations section: ──────────

    int uWinSize  = -1;  // ADD THIS alongside uColor, uOffset, uCellSize


// ── In Renderer.cpp init(), add one line after the other uniform caches: ─────

    uColor    = glGetUniformLocation(shaderProgram, "uColor");
    uOffset   = glGetUniformLocation(shaderProgram, "uOffset");
    uCellSize = glGetUniformLocation(shaderProgram, "uCellSize");
    uWinSize  = glGetUniformLocation(shaderProgram, "uWinSize");  // ADD THIS


// ── In Renderer.cpp draw(), replace the two lines: ───────────────────────────

    // REMOVE these two lines:
    int uWinSize = glGetUniformLocation(shaderProgram, "uWinSize");
    glUniform2f(uWinSize, (float)WIN_W, (float)WIN_H);

    // REPLACE with just:
    glUniform2f(uWinSize, (float)WIN_W, (float)WIN_H);
